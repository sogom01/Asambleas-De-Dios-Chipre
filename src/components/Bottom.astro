---
import "../styles/components/header.css";
import Video from "./Video.astro";
---

<button
  id="videoButton"
  class="fixed bottom-5 left-5 p-3 bg-gradient-to-b from-gray-900 via-gray-800 to-gray-700 text-xl rounded-full opacity-95 flex items-center justify-center shadow-lg hover:bg-blue-700 transition duration-300 z-50 animate-bounce-opacity"
>
  📽️
</button>

<!-- Modal -->
<Video />

<style>
  @keyframes bounceOpacity {
    0%,
    100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(-7px);
      opacity: 0.8;
    }
  }

  .animate-bounce-opacity {
    animation: bounceOpacity 1.5s infinite;
  }

  .modal-content video {
    width: 100%;
  }
</style>

<script src="../js/header.js"></script>
